#TMSH-VERSION: 15.1.10.3
# Negative test: Invalid keyword placement - should fail or warn

sys global-settings {
    hostname f5_bigip_structured_negative_invalid_keyword
}

# This should fail - using ltm keyword incorrectly
ltm {
    pool test_pool {
        members {
            server1:80 {}
        }
    }
}
